# DRS Rules

Configure DRS, affinity rules, and resource pools.

Use the vsphere-architect skill to cover:
- DRS automation levels: Manual, Partially Automated, Fully Automated
- Migration threshold (1-5) and what each level means
- Affinity rule types: VM-VM, VM-Host, affinity vs anti-affinity
- Required vs Preferred rules and HA interaction
- Resource pool design: reservations, limits, shares, expandable reservations
- Common patterns: SQL Always On separation, license compliance, workload isolation

$ARGUMENTS
